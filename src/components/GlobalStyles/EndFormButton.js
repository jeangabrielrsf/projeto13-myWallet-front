import styled from "styled-components";

const EndFormButton = styled.div`
	p {
		color: #ffffff;
		font-size: 15px;
		line-height: 17.5px;
		font-weight: 700;
	}

	display: flex;
	justify-content: center;
	align-items: center;
	margin: 25px auto;

	p:hover {
		cursor: pointer;
	}
`;

export default EndFormButton;
